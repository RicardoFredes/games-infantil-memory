import { registerActivity } from './registry';

// Stub — implementação completa virá na Fase 2.
registerActivity({
  name: 'dance',
  kind: 'toggle',
  durationMs: 6000,
  mood: 'excited',
  start: () => () => {},
});
